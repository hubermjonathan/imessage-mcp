{
  "name": "imessage-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal stdio MCP server with a single tool that sends an iMessage (macOS only).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc && cp src/send.applescript dist/",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
