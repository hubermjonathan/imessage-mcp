{
  "name": "imessage-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal stdio MCP server with a single tool that texts you on iMessage (macOS only).",
  "type": "module",
  "bin": {
    "imessage-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
